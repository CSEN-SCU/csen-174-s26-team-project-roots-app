# Copy to .env.local (gitignored). Never commit real secrets.

# Required for POST /api/extract
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: per-IP limit on extraction (calls Anthropic + geocoding)
# RATE_LIMIT_EXTRACT=5 per hour
